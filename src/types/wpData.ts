export type wpData = {
    phone: string;
    wpLink: string;
    name?: string;
    countryDialCode?: string;
};

export type countryCode = {
    code: string;
    country: string;
    dialCode: string;
};

