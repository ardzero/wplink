export type wpData = {
    phone: string;
    name?: string;
    wpLink?: string;
    country?: string;
};

export type countryCode = {
    code: string;
    country: string;
    dialCode: string;
};

